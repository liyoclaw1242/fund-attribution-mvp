services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data/sitca_raw:/app/data/sitca_raw
      - ./output:/app/output
    env_file:
      - .env
    restart: unless-stopped
