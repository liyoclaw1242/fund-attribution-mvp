"""Optional SQLAlchemy table models for reference.

These are not used at runtime — the pipeline uses raw SQL via asyncpg.
They serve as documentation of the schema for IDE auto-complete and
type checking.
"""

# Placeholder — populate with SQLAlchemy ORM models if needed in the future.
# The canonical schema definition is pipeline/schema.sql.
